version: '3.8'

services:
  smart-qc-studio:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: smart-qc-studio
    ports:
      - "8080:80"
    restart: unless-stopped
    networks:
      - qc-network

networks:
  qc-network:
    driver: bridge
